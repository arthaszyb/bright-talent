[project]
name = "ci"
version = "0.1.0"
description = "Skill-repo CI gate scripts (detect-release, lint, version-check)."
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
