"""DE fleet governance console.

Read-mostly UI + API over the DE demo repo's `instances/*` trees, plus a
change-draft workflow that never writes to the real instance working trees
(all mutation happens in isolated temp workspaces + local git branches).
"""
